# Feedback

The first impression is a fun, run-able and bug free game. Additionally, the code is 
written in a good style and great level of abstraction. Well done!

For the feedback I will collect some bullet point here and write in code comments 
marked with # TODO. You don't need to fulfill all todo's it is just to highlight the 
comments in the IDE. Occasionally I might rewrite some code to highlight best practices. 

- [ ] there should be only one `requirements.txt` at the top level. Currently, `pip install -r requirements.txt` is not working
- [ ] delete not used files
- [ ] structure the code in a dedicated `src` directory
- [ ] double check code quality with `ruff check` - 2 (auto-fixable) errors 

Some of the comments might be outdated - great to see you working on this! :)
